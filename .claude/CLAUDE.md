# User-Level Claude Code Configuration

This file contains user-level instructions that apply across all projects.

## Preferences

- Prefer concise responses unless asked for detail
- Use conventional commit format for git commits
- Do not include "Generated with Claude Code" in commit messages

## Common Patterns

- When working with StudyTrax, follow Angular conventions in the CLAUDE.md files
- Use Jira issue keys (ST-XXXX) in commit messages when applicable
